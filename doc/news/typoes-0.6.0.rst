**Removed:**

* Removed ``inchikey`` from YAML metadata in ``literature/svgdigitizer/**/*.yaml``.

**Fixed:**

* Fixed missing bibliography metadata for raw source-data entries by passing
  ``--bibliography`` in the Makefile conversion rule.
